import { DefaultTheme } from 'react-native-paper'

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    text: '#008600',
    primary: '#008600',
    secondary: '#008600',
    error: 'red',
  },
}
